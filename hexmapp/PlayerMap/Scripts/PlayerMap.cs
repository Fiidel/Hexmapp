using Godot;

public partial class PlayerMap : Node2D
{
	// TODO: draw 1 tile with the selected tile brush
		// a. set a tile on click
		// b. draw the tile on the offset grid using the tile textures and alpha masks
		// c. implement strokes (rather than just setting 1 tile)
		// d. implement erasing tiles
		// e. implement it as a command that gathers all drawn tiles

	
}
